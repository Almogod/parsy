# ── Core API ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
sse-starlette==2.1.0
httpx==0.27.0

# ── Document parsing ───────────────────────────────────────────────────────
PyMuPDF==1.24.3          # PDF: text + image extraction
python-docx==1.1.2       # DOCX parsing
pdfplumber==0.11.1       # PDF table extraction
beautifulsoup4==4.12.3   # HTML parsing
lxml==5.2.2              # Fast XML / HTML backend
chardet==5.2.0           # Encoding detection
openpyxl==3.1.3          # XLSX parsing
Pillow==10.3.0           # Image handling

# ── OCR ────────────────────────────────────────────────────────────────────
pytesseract==0.3.10      # Tesseract OCR wrapper
pdf2image==1.17.0        # PDF → image rasterisation

# ── Phase 1: Worker queue + resource isolation ─────────────────────────────
celery[redis]==5.3.6     # Distributed task queue
redis==5.0.4             # Redis client
kombu==5.3.5             # Celery transport layer

# ── Phase 2: ML models (all run locally) ──────────────────────────────────
transformers==4.41.2             # DiT (layout) + TATR (table structure)
sentence-transformers==3.0.1     # MiniLM-L6 semantic embeddings
onnxruntime==1.18.0              # ONNX Runtime for fast inference
scikit-learn==1.5.0              # K-Means clustering for heading groups
torch==2.3.1                     # PyTorch (CPU-only install recommended)
# Install CPU-only torch: pip install torch --index-url https://download.pytorch.org/whl/cpu

# ── Observability ──────────────────────────────────────────────────────────
prometheus-client==0.20.0
structlog==24.1.0

# ── Utilities ──────────────────────────────────────────────────────────────
python-dotenv==1.0.1
anyio==4.4.0
