# Frontend static files
# This directory contains: index.html, app.js, parser.js, style.css, style-v3.css
# Served by Nginx in production (see nginx.conf and docker-compose.yml)
#
# Development: open index.html directly in a browser, or use:
#   npx serve .
#   python -m http.server 3000
